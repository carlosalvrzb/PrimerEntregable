<!DOCTYPE html>
<html>
  <head>
    <title>Tienda en linea</title>
  </head>
  <body>
    <h1>Productos</h1>
    <ul>
      {{#each products}}
      <li>{{this.name}}</li>
      {{/each}}
    </ul>
  </body>
</html>